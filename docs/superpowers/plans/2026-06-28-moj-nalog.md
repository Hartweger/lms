# "Moj nalog" — stranica polaznika — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dati polazniku jednu stranicu (`/nalog`) gde vidi pristup kursevima sa istekom i obnovom, istoriju porudžbina sa računom (gde postoji), grupni termin + Meet, stanje 1:1 paketa, i profil sa promenom mejla — uz vidljive istekle kurseve i topao "još nisi počeo" ekran na dashboardu radi aktivacije.

**Architecture:** Nova ruta `/nalog` kao server component koja čita podatke. Pristup-zavisni podaci (orders, course_access, individual_enrollments) idu direktno preko RLS-a (`createClient()`). Grupni podaci (groups/group_sessions/group_enrollments imaju staff-only RLS) idu preko novog `/api/student/account` koji koristi admin klijent skovan na ulogovanog korisnika. Čista logika (status isteka, preostali časovi) izdvojena u `src/lib/account.ts` sa vitest testovima. Dashboard se menja da istekle kurseve prikaže zatamnjeno (ne da ih sakrije) i da na 0% pokaže onboarding.

**Tech Stack:** Next.js 16 (App Router, Turbopack — vidi `node_modules/next/dist/docs/` pre koda), Supabase SSR (`@supabase/ssr`), TypeScript, Tailwind, vitest.

**VAŽNO pre koda:** Ovo je produkcioni repo (`hartweger.rs`) na grani `main` sa već postojećim necommitovanim izmenama drugog posla. Raditi na zasebnoj grani `feat/moj-nalog` (ili worktree), NE direktno na prljavom `main`. Deploy je ručni `vercel --prod` + obavezni smoke test (vidi memoriju). Crtica je obična, "ti" forma svuda, potpis/ton topao.

---

## File Structure

- Create: `src/lib/account.ts` — čiste funkcije: status pristupa, preostali časovi, da li prikazati "Obnovi".
- Create: `src/lib/account.test.ts` — vitest za gornje.
- Create: `src/app/api/student/account/route.ts` — GET; vraća grupni termin + Meet + 1:1 stanje za ulogovanog korisnika (admin klijent, skovan na `auth.uid()`).
- Create: `src/app/nalog/page.tsx` — server component, glavna stranica "Moj nalog".
- Create: `src/app/nalog/Sekcije.tsx` — client componente za interaktivne delove (promena mejla, kopiranje Meet linka).
- Modify: `src/app/dashboard/page.tsx` — istekli kursevi vidljivi (zatamnjeno) + 0% onboarding stanje.
- Modify: nav (NavClient) — dodati link "Moj nalog" → `/nalog`.
- Modify: `src/app/profil/page.tsx` — izbaciti brisanje naloga; (profil polja se sele/duplikuju u `/nalog`, vidi Task 8 napomenu).
- Reference (ne menjati): `supabase/migrations/021_marketing_tables.sql` (orders), `010_course_access_expires.sql`, `035_groups.sql`, `040_individual_courses.sql`, `043_review_fixes.sql`.

---

## Potvrđene činjenice iz baze (ne nagađati)

- `orders`: `user_id`, `email`, `items` (JSONB), `total` (INT, RSD), `payment_method` (`kartica`|`kartica_rate`|`uplatnica`|`paypal`), `payment_status` (`pending`|`completed`|`failed`), `order_number`, `created_at`, **`fiscal_pdf_url`** (URL računa — može biti NULL), `fiscalized_at`. RLS: "Users can read own orders" (`auth.uid() = user_id`).
- `course_access`: `user_id`, `course_id`, `granted_at`, **`expires_at`** (NULL = bez roka), `source`. RLS čita svoje.
- `individual_enrollments`: `user_id`, `course_id`, `professor_id`, `order_id`, **`package_lessons`**, **`lessons_used`**, `status` (`active|completed|expired|cancelled`), `expires_at`, `notes_doc_url`. RLS: "ind_enroll student read own" (`auth.uid() = user_id`).
- `group_enrollments`: `user_id`, `group_id`, `status` (`active|cancelled`). **RLS staff-only → čitati preko API admin klijenta.**
- `groups`: `level`, `status`, `start_date`, `days` (smallint[]), `session_time` (npr "18:00-19:00"), **`meet_link`**, `professor_id`, `content_course_id`.
- `group_sessions`: `group_id`, `session_date` (DATE), `cancelled` (bool). "Sledeći termin" = najmanji `session_date >= danas` i `cancelled=false`.
- Profesor ima `calendar_url` (`user_profiles.calendar_url`) — koristi se za 1:1 "Zakaži".
- Auth callback postoji: `src/app/auth/callback` — koristi se za potvrdu promene mejla.
- Supabase server klijent: `import { createClient } from "@/lib/supabase/server"` → `const supabase = await createClient()`. Client: `import { createClient } from "@/lib/supabase/client"`. Admin: `createAdminClient()` (vidi `src/app/admin/narudzbine/page.tsx`).

---

### Task 1: Čista logika pristupa + testovi

**Files:**
- Create: `src/lib/account.ts`
- Test: `src/lib/account.test.ts`

- [ ] **Step 1: Napiši test koji pada**

```ts
import { describe, it, expect } from "vitest";
import { accessStatus, remainingSessions, shouldShowRenew } from "./account";

const NOW = new Date("2026-06-28T10:00:00Z");

describe("accessStatus", () => {
  it("bez roka → trajan, ne prikazuje istek", () => {
    expect(accessStatus(null, NOW)).toEqual({ state: "none", daysLeft: null });
  });
  it("rok za 18 dana → aktivan", () => {
    expect(accessStatus("2026-07-16T10:00:00Z", NOW)).toEqual({ state: "active", daysLeft: 18 });
  });
  it("rok za 5 dana → uskoro istice", () => {
    expect(accessStatus("2026-07-03T10:00:00Z", NOW)).toEqual({ state: "expiring", daysLeft: 5 });
  });
  it("rok prosao → istekao", () => {
    expect(accessStatus("2026-06-20T10:00:00Z", NOW)).toEqual({ state: "expired", daysLeft: -8 });
  });
});

describe("remainingSessions", () => {
  it("paket 8, iskorisceno 3 → 5", () => {
    expect(remainingSessions(8, 3)).toBe(5);
  });
  it("ne ide ispod 0", () => {
    expect(remainingSessions(8, 10)).toBe(0);
  });
});

describe("shouldShowRenew", () => {
  it("prikazuje obnovu samo kad postoji rok i nije trajan", () => {
    expect(shouldShowRenew({ state: "expiring", daysLeft: 5 })).toBe(true);
    expect(shouldShowRenew({ state: "expired", daysLeft: -1 })).toBe(true);
    expect(shouldShowRenew({ state: "active", daysLeft: 40 })).toBe(false);
    expect(shouldShowRenew({ state: "none", daysLeft: null })).toBe(false);
  });
});
```

- [ ] **Step 2: Pokreni test, potvrdi da pada**

Run: `npx vitest run src/lib/account.test.ts`
Expected: FAIL ("does not provide an export named 'accessStatus'").

- [ ] **Step 3: Napiši minimalnu implementaciju**

```ts
export type AccessState = "none" | "active" | "expiring" | "expired";
export interface AccessStatus { state: AccessState; daysLeft: number | null; }

const DAY = 86_400_000;
const EXPIRING_THRESHOLD_DAYS = 7;

export function accessStatus(expiresAt: string | null, now: Date = new Date()): AccessStatus {
  if (!expiresAt) return { state: "none", daysLeft: null };
  const daysLeft = Math.round((new Date(expiresAt).getTime() - now.getTime()) / DAY);
  if (daysLeft < 0) return { state: "expired", daysLeft };
  if (daysLeft <= EXPIRING_THRESHOLD_DAYS) return { state: "expiring", daysLeft };
  return { state: "active", daysLeft };
}

export function remainingSessions(packageLessons: number, lessonsUsed: number): number {
  return Math.max(0, packageLessons - lessonsUsed);
}

export function shouldShowRenew(s: AccessStatus): boolean {
  return s.state === "expiring" || s.state === "expired";
}
```

- [ ] **Step 4: Pokreni test, potvrdi prolaz**

Run: `npx vitest run src/lib/account.test.ts`
Expected: PASS (sve).

- [ ] **Step 5: Commit**

```bash
git add src/lib/account.ts src/lib/account.test.ts
git commit -m "feat(nalog): cista logika statusa pristupa i preostalih casova"
```

---

### Task 2: API ruta za grupni termin + 1:1 stanje

**Files:**
- Create: `src/app/api/student/account/route.ts`

Razlog: `group_enrollments`/`groups`/`group_sessions` imaju staff-only RLS, pa se čitaju admin klijentom, ali striktno skovano na ulogovanog korisnika. 1:1 ima student RLS, ali ga vraćamo ovde da stranica ima jedan izvor.

- [ ] **Step 1: Implementiraj GET rutu**

```ts
import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
import { createAdminClient } from "@/lib/supabase/admin";

export async function GET() {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return NextResponse.json({ error: "unauthorized" }, { status: 401 });

  const admin = createAdminClient();
  const today = new Date().toISOString().slice(0, 10);

  // Grupni: aktivni upis -> grupa -> sledeci termin
  const { data: enrolls } = await admin
    .from("group_enrollments")
    .select("group_id")
    .eq("user_id", user.id)
    .eq("status", "active");

  const groupIds = (enrolls ?? []).map((e) => e.group_id);
  let groups: Array<{ id: string; level: string; session_time: string | null; meet_link: string | null; nextSession: string | null; professor: string | null }> = [];

  if (groupIds.length) {
    const { data: groupRows } = await admin
      .from("groups")
      .select("id, level, session_time, meet_link, professor:professor_id(full_name)")
      .in("id", groupIds);
    const { data: sessions } = await admin
      .from("group_sessions")
      .select("group_id, session_date")
      .in("group_id", groupIds)
      .gte("session_date", today)
      .eq("cancelled", false)
      .order("session_date", { ascending: true });

    groups = (groupRows ?? []).map((g) => {
      const next = (sessions ?? []).find((s) => s.group_id === g.id);
      const prof = Array.isArray(g.professor) ? g.professor[0] : g.professor;
      return {
        id: g.id,
        level: g.level,
        session_time: g.session_time,
        meet_link: g.meet_link,
        nextSession: next?.session_date ?? null,
        professor: prof?.full_name ?? null,
      };
    });
  }

  // 1:1 paketi (preko admina radi jednog izvora; filtrirano po user.id)
  const { data: indiv } = await admin
    .from("individual_enrollments")
    .select("id, course_id, package_lessons, lessons_used, status, expires_at, professor:professor_id(full_name, calendar_url), course:course_id(title, slug)")
    .eq("user_id", user.id)
    .in("status", ["active", "completed"]);

  const individual = (indiv ?? []).map((e) => {
    const prof = Array.isArray(e.professor) ? e.professor[0] : e.professor;
    const course = Array.isArray(e.course) ? e.course[0] : e.course;
    return {
      id: e.id,
      title: course?.title ?? "Individualni 1:1",
      packageLessons: e.package_lessons,
      lessonsUsed: e.lessons_used,
      status: e.status,
      expiresAt: e.expires_at,
      professor: prof?.full_name ?? null,
      calendarUrl: prof?.calendar_url ?? null,
    };
  });

  return NextResponse.json({ groups, individual });
}
```

- [ ] **Step 2: Proveri da admin klijent postoji**

Run: `grep -rn "createAdminClient" src/lib/supabase/ src/app/admin/narudzbine/page.tsx | head`
Expected: nalazi izvor `createAdminClient` (npr `@/lib/supabase/admin`). Ako je putanja drugačija, ispravi import u Step 1.

- [ ] **Step 3: Smoke (build prolazi)**

Run: `npx tsc --noEmit`
Expected: bez grešaka u `route.ts` (tipovi join-ova mogu da traže `as` cast; ako TS prijavi, dodaj eksplicitan tip umesto `any`).

- [ ] **Step 4: Commit**

```bash
git add src/app/api/student/account/route.ts
git commit -m "feat(nalog): API za grupni termin i 1:1 stanje (admin skovano na korisnika)"
```

---

### Task 3: Stranica `/nalog` — skelet + nav link + profil + kursevi

**Files:**
- Create: `src/app/nalog/page.tsx`
- Create: `src/app/nalog/Sekcije.tsx`
- Modify: nav komponenta (pronaći: `grep -rln "Moji kursevi\|/dashboard" src/components`)

- [ ] **Step 1: Server stranica čita podatke pod RLS-om**

```tsx
import { createClient } from "@/lib/supabase/server";
import { redirect } from "next/navigation";
import { accessStatus } from "@/lib/account";
import { ProfilSekcija, GrupniIIndividualni } from "./Sekcije";

export const metadata = { title: "Moj nalog" };

export default async function NalogPage() {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) redirect("/prijava");

  const { data: profile } = await supabase.from("user_profiles").select("full_name").eq("id", user.id).single();

  const { data: accessList } = await supabase
    .from("course_access")
    .select("course_id, expires_at")
    .eq("user_id", user.id);
  const courseIds = (accessList ?? []).map((a) => a.course_id);
  const { data: courses } = courseIds.length
    ? await supabase.from("courses").select("id, title, slug, category").in("id", courseIds)
    : { data: [] as Array<{ id: string; title: string; slug: string; category: string | null }> };

  const now = new Date();
  const kursevi = (courses ?? []).map((c) => {
    const acc = (accessList ?? []).find((a) => a.course_id === c.id);
    return { ...c, status: accessStatus(acc?.expires_at ?? null, now) };
  });

  const { data: orders } = await supabase
    .from("orders")
    .select("id, order_number, items, total, payment_method, payment_status, created_at, fiscal_pdf_url")
    .eq("payment_status", "completed")
    .order("created_at", { ascending: false });

  return (
    <div className="max-w-2xl mx-auto px-4 py-10">
      <h1 className="text-2xl font-bold text-gray-900 mb-6">Moj nalog</h1>
      {/* Kursevi sa istekom/obnovom (Task 4) renderuje se ovde iz `kursevi` */}
      {/* Porudzbine (Task 5) iz `orders` */}
      <GrupniIIndividualni />
      <ProfilSekcija initialName={profile?.full_name ?? ""} email={user.email ?? ""} />
    </div>
  );
}
```

- [ ] **Step 2: `Sekcije.tsx` — klijentski deo: grupni/1:1 fetch + profil**

```tsx
"use client";
import { useEffect, useState } from "react";

export function GrupniIIndividualni() {
  const [data, setData] = useState<{ groups: any[]; individual: any[] } | null>(null);
  useEffect(() => { fetch("/api/student/account").then((r) => r.ok ? r.json() : { groups: [], individual: [] }).then(setData); }, []);
  if (!data || (!data.groups.length && !data.individual.length)) return null;
  return (
    <section className="mb-8">
      {data.groups.map((g) => (
        <div key={g.id} className="border border-gray-200 rounded-lg p-4 mb-2">
          <p className="font-medium">Grupni {g.level}{g.professor ? ` · ${g.professor}` : ""}</p>
          <p className="text-sm text-gray-600">
            {g.nextSession ? `Sledeći čas: ${new Date(g.nextSession).toLocaleDateString("sr-RS")}${g.session_time ? ` u ${g.session_time}` : ""}` : "Termin uskoro"}
          </p>
          {g.meet_link && <a href={g.meet_link} target="_blank" rel="noreferrer" className="inline-block mt-2 text-sm text-plava">Otvori Meet</a>}
        </div>
      ))}
      {data.individual.map((e) => {
        const remaining = Math.max(0, e.packageLessons - e.lessonsUsed);
        return (
          <div key={e.id} className="border border-gray-200 rounded-lg p-4 mb-2">
            <p className="font-medium">{e.title}{e.professor ? ` · ${e.professor}` : ""}</p>
            <p className="text-sm text-gray-600">Ostalo ti {remaining} od {e.packageLessons} časova</p>
            {remaining > 0 && e.calendarUrl && <a href={e.calendarUrl} target="_blank" rel="noreferrer" className="inline-block mt-2 text-sm text-plava">Zakaži sledeći čas</a>}
          </div>
        );
      })}
    </section>
  );
}

export function ProfilSekcija({ initialName, email }: { initialName: string; email: string }) {
  // Implementira se u Task 8 (ime + promena mejla + lozinka). Privremeno: prikazi ime/email.
  return (
    <section className="mt-8 pt-8 border-t border-gray-100">
      <h2 className="text-base font-semibold mb-3">Profil</h2>
      <p className="text-sm text-gray-600">Ime: {initialName || "—"}</p>
      <p className="text-sm text-gray-600">Email: {email}</p>
    </section>
  );
}
```

- [ ] **Step 3: Dodaj nav link**

Pronađi nav: `grep -rln "Moji kursevi" src/components`. U toj komponenti, pored "Moji kursevi" → `/dashboard`, dodaj `<Link href="/nalog">Moj nalog</Link>` istog stila.

- [ ] **Step 4: Build + ručni smoke**

Run: `npm run dev` pa otvori `http://localhost:3000/nalog` ulogovan kao polaznik (ili `npx tsc --noEmit` ako nema test naloga).
Expected: stranica se renderuje, grupni/1:1 sekcija se popuni ako korisnik ima upise.

- [ ] **Step 5: Commit**

```bash
git add src/app/nalog/ src/components
git commit -m "feat(nalog): skelet stranice, grupni+1:1 sekcija, nav link"
```

---

### Task 4: Sekcija "Pristup i kursevi" (aktivni + istekli + obnovi)

**Files:**
- Modify: `src/app/nalog/page.tsx` (render `kursevi`)

Pravilo: red o pristupu se prikazuje SAMO kad `status.state !== "none"`. "Obnovi" SAMO kad `shouldShowRenew`. Istekli kursevi su zatamnjeni, ne sakriveni.

- [ ] **Step 1: Pronađi kupon flow za "Obnovi"**

Run: `grep -rn "kupon\|coupon\|OBNOVI50" src/app/kupovina/[slug]/page.tsx src/app/api/orders/route.ts | head`
Cilj: utvrdi prima li checkout kupon iz URL-a (npr `?kupon=OBNOVI50`). Ako prima → "Obnovi" link = `/kupovina/${slug}?kupon=OBNOVI50`. Ako NE prima → link = `/kupovina/${slug}` i dodaj podršku za `searchParams.kupon` u checkout (pred-popuni polje kupona). Zabeleži tačan naziv parametra ovde pre nego što napišeš link.

- [ ] **Step 2: Render kurseva sa uslovnim istekom/obnovom**

U `page.tsx`, na mestu komentara iz Task 3, dodaj (uz `import { shouldShowRenew } from "@/lib/account"`):

```tsx
<section className="mb-8">
  <p className="text-sm font-medium text-gray-500 mb-2">Pristup i kursevi</p>
  {kursevi.map((c) => {
    const expired = c.status.state === "expired";
    return (
      <div key={c.id} className={`border rounded-lg p-4 mb-2 ${expired ? "border-gray-200 bg-gray-50 opacity-70" : "border-gray-200"}`}>
        <p className="font-medium">{c.title}</p>
        {c.status.state === "active" && c.status.daysLeft! <= 30 && (
          <p className="text-sm text-gray-500 mt-1">Pristup ističe za {c.status.daysLeft} dana</p>
        )}
        {c.status.state === "expiring" && (
          <p className="text-sm text-koral-dark mt-1">Pristup ističe za {c.status.daysLeft} dana</p>
        )}
        {expired && <p className="text-sm text-koral-dark mt-1">Pristup je istekao</p>}
        {shouldShowRenew(c.status) && (
          <a href={`/kupovina/${c.slug}?kupon=OBNOVI50`} className="inline-block mt-2 text-sm bg-koral-light text-koral-dark px-3 py-1.5 rounded-lg">Obnovi −50%</a>
        )}
      </div>
    );
  })}
  {kursevi.length === 0 && <p className="text-sm text-gray-500">Još nemaš kurseve. <a href="/kursevi" className="text-plava">Pogledaj ponudu</a></p>}
</section>
```

(Ako Step 1 pokaže drugačiji naziv parametra, zameni `?kupon=OBNOVI50`.)

- [ ] **Step 3: Build + smoke**

Run: `npx tsc --noEmit && npm run build`
Expected: prolazi. Ručno: kurs sa rokom <7 dana koral + "Obnovi"; kurs bez roka samo naziv; istekao zatamnjen + "Obnovi".

- [ ] **Step 4: Commit**

```bash
git add src/app/nalog/page.tsx src/app/kupovina
git commit -m "feat(nalog): sekcija pristup/kursevi sa uslovnim istekom i obnovom"
```

---

### Task 5: Sekcija "Moje porudžbine" (uslovni račun)

**Files:**
- Modify: `src/app/nalog/page.tsx`

Pravilo: dugme "Račun" se prikazuje SAMO ako `order.fiscal_pdf_url` postoji. Inače reda nema (porudžbina se i dalje vidi, bez dugmeta).

- [ ] **Step 1: Render porudžbina**

```tsx
<section className="mb-8">
  <p className="text-sm font-medium text-gray-500 mb-2">Moje porudžbine</p>
  <div className="border border-gray-200 rounded-lg divide-y divide-gray-100">
    {(orders ?? []).map((o) => {
      const naziv = Array.isArray(o.items) && o.items[0]?.title ? o.items[0].title : "Porudžbina";
      return (
        <div key={o.id} className="flex items-center justify-between gap-3 px-4 py-3">
          <div>
            <p className="text-sm font-medium">{naziv}</p>
            <p className="text-xs text-gray-500">{new Date(o.created_at).toLocaleDateString("sr-RS")} · {o.payment_method} · plaćeno</p>
          </div>
          <div className="flex items-center gap-3">
            <span className="text-sm font-medium">{o.total} €</span>
            {o.fiscal_pdf_url && <a href={o.fiscal_pdf_url} target="_blank" rel="noreferrer" className="text-xs text-plava border border-gray-200 rounded-md px-2 py-1">Račun</a>}
          </div>
        </div>
      );
    })}
    {(orders ?? []).length === 0 && <p className="text-sm text-gray-500 px-4 py-3">Nemaš porudžbina.</p>}
  </div>
</section>
```

NAPOMENA: proveri valutu — `orders.total` je INT u RSD (ne €). Ako je RSD, prikaži `{o.total.toLocaleString("sr-RS")} RSD`. Potvrdi čitanjem jedne porudžbine u bazi pre finalizacije teksta valute.

- [ ] **Step 2: Potvrdi valutu/oblik items**

Run: `grep -rn "total\|items\|currency\|RSD\|EUR" src/app/kupovina/hvala/[orderId]/page.tsx | head`
Expected: vidiš kako thank-you stranica prikazuje iznos i `items[].title`; uskladi format ovde.

- [ ] **Step 3: Build + smoke**

Run: `npx tsc --noEmit`
Expected: prolazi; porudžbina sa `fiscal_pdf_url` ima dugme, bez njega nema.

- [ ] **Step 4: Commit**

```bash
git add src/app/nalog/page.tsx
git commit -m "feat(nalog): porudzbine sa uslovnim racunom"
```

---

### Task 6: Profil + promena mejla

**Files:**
- Modify: `src/app/nalog/Sekcije.tsx` (`ProfilSekcija`)
- Modify: `src/app/profil/page.tsx` (izbaci brisanje naloga)

- [ ] **Step 1: Implementiraj ProfilSekcija (ime + email + lozinka)**

Zameni privremeni `ProfilSekcija` punim (ime se čuva u `user_profiles`, email preko `supabase.auth.updateUser({ email })`):

```tsx
"use client";
import { useState } from "react";
import { createClient } from "@/lib/supabase/client";

export function ProfilSekcija({ initialName, email }: { initialName: string; email: string }) {
  const supabase = createClient();
  const [name, setName] = useState(initialName);
  const [newEmail, setNewEmail] = useState(email);
  const [msg, setMsg] = useState("");

  const saveName = async () => {
    const { data: { user } } = await supabase.auth.getUser();
    if (!user) return;
    await supabase.from("user_profiles").update({ full_name: name }).eq("id", user.id);
    setMsg("Ime sačuvano.");
  };
  const changeEmail = async () => {
    if (newEmail === email) return;
    const { error } = await supabase.auth.updateUser(
      { email: newEmail },
      { emailRedirectTo: `${window.location.origin}/auth/callback` }
    );
    setMsg(error ? "Greška pri promeni mejla." : "Poslali smo link za potvrdu na stari i novi mejl.");
  };

  return (
    <section className="mt-8 pt-8 border-t border-gray-100">
      <h2 className="text-base font-semibold mb-3">Profil</h2>
      <label className="block text-sm text-gray-600 mb-1">Ime i prezime</label>
      <input value={name} onChange={(e) => setName(e.target.value)} className="w-full px-4 py-3 border border-gray-200 rounded-lg mb-2" />
      <button onClick={saveName} className="text-sm bg-plava text-white px-4 py-2 rounded-lg mb-4">Sačuvaj ime</button>

      <label className="block text-sm text-gray-600 mb-1">Email</label>
      <div className="flex gap-2 items-center">
        <input value={newEmail} onChange={(e) => setNewEmail(e.target.value)} className="flex-1 px-4 py-3 border border-gray-200 rounded-lg" />
        <button onClick={changeEmail} className="text-sm border border-gray-300 px-4 py-2 rounded-lg whitespace-nowrap">Promeni</button>
      </div>
      <p className="text-xs text-gray-400 mt-1">Poslaćemo link za potvrdu na stari i novi mejl.</p>

      {msg && <p className="text-sm text-plava-dark mt-3">{msg}</p>}

      <p className="text-sm mt-4"><a href="/profil" className="text-plava">Promeni lozinku</a></p>
    </section>
  );
}
```

- [ ] **Step 2: Potvrdi da Supabase "Secure email change" radi (dupla potvrda)**

Run: `grep -rn "updateUser\|email" src/app/auth/callback/route.ts | head`
Expected: callback razmenjuje kod/sesiju; promena mejla traži potvrdu na oba mejla ako je u Supabase Auth uključeno "Secure email change" (podrazumevano). Ako nije, zabeleži za ručno uključivanje u Supabase dashboardu (Auth → Email).

- [ ] **Step 3: Izbaci brisanje naloga iz `/profil`**

U `src/app/profil/page.tsx` obriši `handleDeleteAccount` i ceo blok "Brisanje naloga" (poslednji `<div className="mt-12 ...">`). Razlog: trenutno dugme ionako ne briše podatke (samo izloguje), a računi se moraju čuvati (fiskalizacija). Pravo na brisanje pokriva politika privatnosti (na zahtev mejlom).

- [ ] **Step 4: Build + smoke**

Run: `npx tsc --noEmit && npm run build`
Expected: prolazi; promena imena radi; "Promeni" mejl prikazuje poruku o potvrdi; `/profil` više nema brisanje.

- [ ] **Step 5: Commit**

```bash
git add src/app/nalog/Sekcije.tsx src/app/profil/page.tsx
git commit -m "feat(nalog): profil sa promenom mejla; izbaceno brisanje naloga"
```

---

### Task 7: Dashboard — istekli kursevi vidljivi + "još nisi počeo" (0%)

**Files:**
- Modify: `src/app/dashboard/page.tsx`

- [ ] **Step 1: Ne filtriraj istekle — označi ih**

Pronađi blok (oko linija 123-133) gde se `course_access` filtrira `!a.expires_at || a.expires_at >= now`. Zameni tako da se uzmu SVI `course_id`, a istek se nosi dalje:

```tsx
const { data: accessList } = await supabase
  .from("course_access")
  .select("course_id, expires_at")
  .eq("user_id", user.id);
const nowIso = new Date().toISOString();
const expiredSet = new Set(
  (accessList ?? []).filter((a) => a.expires_at && a.expires_at < nowIso).map((a) => a.course_id)
);
courseIds = (accessList ?? []).map((a) => a.course_id); // svi, ukljucujuci istekle
```

Zatim, gde se renderuje svaki kurs, dodaj zatamnjenje + "Obnovi" kad `expiredSet.has(course.id)`:

```tsx
const expired = expiredSet.has(course.id);
// u JSX kartice:
className={`... ${expired ? "opacity-60" : ""}`}
{expired && (
  <a href={`/kupovina/${course.slug}?kupon=OBNOVI50`} className="text-sm text-koral-dark">Pristup istekao · Obnovi −50%</a>
)}
```

(Naziv kupon-parametra uskladiti sa Task 4 Step 1.)

- [ ] **Step 2: 0% onboarding stanje**

Za kurs gde je napredak 0 (nijedna lekcija završena), umesto praznog progress bara prikaži topao CTA:

```tsx
{progressPct === 0 ? (
  <a href={`/kurs/${course.slug}`} className="block bg-plava-light text-plava-dark rounded-lg p-3 text-sm font-medium">
    Tvoj prvi čas te čeka — počni ovde
  </a>
) : (
  /* postojeci progress bar */
)}
```

(Uskladi `progressPct` sa stvarnim imenom promenljive u fajlu — pronađi: `grep -n "progress\|Pct\|completed" src/app/dashboard/page.tsx`.)

- [ ] **Step 3: Build + smoke**

Run: `npx tsc --noEmit && npm run build`
Expected: prolazi; istekao kurs se vidi zatamnjeno sa "Obnovi"; kurs na 0% pokazuje "počni" CTA umesto praznog bara.

- [ ] **Step 4: Commit**

```bash
git add src/app/dashboard/page.tsx
git commit -m "feat(dashboard): istekli kursevi vidljivi + onboarding na 0%"
```

---

### Task 8: "Pomoć" fusnota + topli prazni ekrani + finalni smoke/deploy

**Files:**
- Modify: `src/app/nalog/page.tsx`

- [ ] **Step 1: Pomoć fusnota na dnu `/nalog`**

```tsx
<p className="text-sm text-gray-400 mt-10 text-center">
  Treba ti pomoć? Piši nam na <a href="mailto:info@hartweger.rs" className="text-plava">info@hartweger.rs</a>
</p>
```

- [ ] **Step 2: Proveri sve prazne slučajeve (KTZ i sl.)**

Ručno proveri: korisnik bez grupe/1:1 → te sekcije se ne prikazuju (već vraćaju `null`); korisnik bez isteka → nema reda o pristupu; porudžbina bez računa → nema dugmeta. KTZ (nije na platformi) se ne pojavljuje jer nema `course_access` red.

- [ ] **Step 3: Pun build + lint**

Run: `npm run build && npx eslint src/app/nalog src/lib/account.ts src/app/api/student/account`
Expected: bez grešaka.

- [ ] **Step 4: Pokreni sve testove**

Run: `npx vitest run`
Expected: PASS (uključujući `account.test.ts`).

- [ ] **Step 5: Commit + (uz dozvolu korisnika) deploy**

```bash
git add src/app/nalog/page.tsx
git commit -m "feat(nalog): pomoc fusnota i prazni ekrani"
```

Deploy: `vercel --prod` iz lokala (ručno, vidi memoriju) → zatim OBAVEZNI smoke test (`/nalog`, `/dashboard`, `/profil`) na produkciji sa cache-busterom.

---

## Self-Review (provereno spram spec-a)

- Istek + obnova → Task 4 + Task 7 (uslovno preko `shouldShowRenew`). ✓
- Račun samo gde postoji → Task 5 (`fiscal_pdf_url` guard). ✓
- Promena mejla (dupla potvrda) → Task 6. ✓
- "Trajan pristup" izbačen → nema reda o pristupu kad `state==="none"`. ✓
- Brisanje naloga izbačeno → Task 6 Step 3. ✓
- Istekli kursevi vidljivi → Task 7 Step 1. ✓
- 0% onboarding → Task 7 Step 2. ✓
- Grupni termin + Meet → Task 2 + Task 3. ✓
- 1:1 preostali + zakaži → Task 2 + Task 3. ✓
- Pomoć fusnota → Task 8. ✓
- Otvorena tačka koja se rešava u toku: tačan naziv kupon-parametra u checkout-u (Task 4 Step 1) i valuta `orders.total` RSD vs € (Task 5 Step 2) — oba imaju konkretan korak provere, ne pretpostavku.
